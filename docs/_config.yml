# PHP-FBR documentation site — built by GitHub Pages (Jekyll) from /docs
# Self-contained template: custom layout + assets in this folder, no external
# theme dependency, so it builds identically on any Jekyll version.
theme: null

title: PHP-FBR
description: Just for fun — file based routing with PHP
lang: en

# Sidebar navigation is generated from each page's front matter:
#   layout: default
#   title: Page Title      -> shown in the sidebar
#   nav_order: N           -> sidebar position (lower first)
#   nav_exclude: true      -> optional, hides the page from the sidebar
footer_content: "PHP-FBR &copy; Rizky Kurniawan — [Source on GitHub](https://github.com/kykurniawan/php-fbr)"
